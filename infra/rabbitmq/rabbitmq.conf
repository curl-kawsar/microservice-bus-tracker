# RabbitMQ Configuration
# Default settings are used via Docker environment variables

# Management plugin enabled by default in rabbitmq:3-management image
# Access at http://localhost:15672 with guest/guest
